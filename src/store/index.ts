import { createStore } from "vuex";
import user from "@/store/user";

export default createStore({
  // state: {},
  // getters: {},
  mutations: {},
  actions: {},
  modules: {
    user, // 注册用户模块
  },
});
